<h2>Lista de postagens:</h2>
<hr>
<a href="/postagens/add">
    <button type="submit" class="btn btn-success">Nova postagem</button>
</a>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{titulo}}</h4>
            <small>Slug: {{slug}}</small>
            <small>Data criação: {{date}}</small>
            <small>Categoria: {{categoria.nome}}</small>
            <a href="/postagens/edit/{{_id}}"><button class="btn btn-success">Editar postagem</button></a>
            <form action="/postagens/deletar" method="post" style="display:inline;">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-danger">Deletar postagem</button>
            </form>
        </div>
    </div>

{{else}}
    <h4 class="mt-4">Nenhuma postagem encontrada</h4>
{{/each}}